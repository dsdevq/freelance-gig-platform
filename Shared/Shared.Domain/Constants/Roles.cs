using Shared.Domain.Enums;

namespace Shared.Domain.Constants;

public static class Roles
{
    public const string Freelancer = nameof(RoleType.Freelancer);
    public const string Client = nameof(RoleType.Client);
} 